# Dependencies
node_modules/

# Build output
dist/
coverage/

# Local environment and secrets
.env
.env.*
!.env.example
.dev.vars

# Wrangler / Cloudflare local state
.wrangler/

# Logs and runtime files
*.log
*.pid
*.pid.lock
logs/
pids/
.pm2/

# Editor and OS files
.DS_Store
.vscode/
.idea/
*.swp

# Archives and backups
*.backup
*.bak
*.tar.gz
*.zip

# Cloudflare Worker local secrets (contains API keys — never commit)
worker/.dev.vars
worker/.wrangler/
worker/dev.vars

# local D1 backups (contain secrets)
worker/.d1_backup_*.sql

# portable DB exports (contain credentials — move privately, never via git)
solent_db_export_*.sql
